{
  "name": "claude-code-starter-app",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal example app for the Claude Code starter kit: a static SPA served by a dependency-free Node http server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
